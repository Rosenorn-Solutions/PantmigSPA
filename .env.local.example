# Prefer DNS domain verification in Google Search Console.
# If you choose HTML tag verification instead, you can expose the token via code.
# Example:
# NEXT_PUBLIC_GSC_VERIFICATION=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# === Email / Contact configuration ===
# SMTP settings for Nodemailer
# If you use a provider like SendGrid, Mailgun, Brevo, or your own SMTP server,
# fill these out and copy this file to .env.local (do not commit .env.local)
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
SMTP_REQUIRE_TLS=false

# Contact email settings
# Default recipient (falls back to pantmig@pantmig.dk if unset)
CONTACT_TO=pantmig@pantmig.dk
# Sender shown on the email (use a verified sender if your provider requires it)
CONTACT_FROM="PantMig <pantmig@pantmig.dk>"

# --- Example: Simply.com SMTP ---
# Outgoing SMTP with STARTTLS on port 587
# SMTP_HOST=smtp.simply.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_REQUIRE_TLS=true
# SMTP_USER=pantmig@pantmig.dk
# SMTP_PASS=<<email_password>>
